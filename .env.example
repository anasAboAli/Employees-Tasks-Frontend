# Decoupled Frontend Production API Configuration
# Set this to your deployed backend URL (e.g. https://your-backend-api.com)
# If left empty, it defaults to the same host domain (relative /api)
VITE_API_URL=
